<header>
  <nav class="navbar navbar-expand-md navbar-dark bg-info">
    <a class="navbar-brand" href="/urls">TinyApp</a>
    <button class="navbar-toggler" type="button" data-toggle="collapse" data-target="#navbarNavAltMarkup"
      aria-controls="navbarNavAltMarkup" aria-expanded="false" aria-label="Toggle navigation">
      <span class="navbar-toggler-icon"></span>
    </button>
    <div class="collapse navbar-collapse" id="navbarNavAltMarkup">
      <div class="navbar-nav">
        <a class="nav-item nav-link" href="/urls">My URLs</a>
        <a class="nav-item nav-link" href="/urls/new">Create New URL</a>
      </div>
      <% if(!user){ %>
        <form class="form-inline ml-auto" action="/login" method="post">
          <input class="form-control mr-2" type="text" name="email" placeholder="Email">
          <button class="btn btn-outline-light" type="submit">Login</button>
        </form>
        <% } else { %>
          <span class="my-auto mr-2 ml-auto text-light">
            Logged in as: <i>
              <%= user.email %>
            </i>
          </span>
          <form action="/logout" method="post">
            <button type="submit" class="btn btn-outline-light ml-2">Logout</button>
          </form>
          <% } %>
    </div>
  </nav>
</header>